# 终端命令规范

## 核心原则
终端使用的是 Git Bash，执行命令时使用 Bash/Unix 风格的命令，而不是 Windows CMD 或 PowerShell 命令。

## 关键要点

- 使用 Unix/Linux 命令：`ls`、`cat`、`rm`、`cp`、`mv` 等
- 路径使用正斜杠 `/`：`cd stock-trading-system/backend`
- 执行脚本使用 `./`：`./mvnw clean install`

## 适用范围
所有终端命令执行和脚本编写。
